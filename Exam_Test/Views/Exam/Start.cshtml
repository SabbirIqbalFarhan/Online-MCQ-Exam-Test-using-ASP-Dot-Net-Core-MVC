@model List<Exam_Test.Models.Question>

@{
    ViewData["Title"] = "Exam";
    int moduleId = ViewBag.ModuleId;
    int sessionId = ViewBag.SessionId;
    string sessionTitle = ViewBag.SessionTitle;
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Module @moduleId Exam</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: Arial, sans-serif; background: #fff; margin: 0; padding: 0; overflow: hidden; }

        .page-wrapper {
    width: 100vw;
    min-height: 100vh;
    background: #e0e0e0;
    display: flex;
    flex-direction: column;
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    overflow-y: auto;
    padding-bottom: 130px;
}

        /* TOP BAR */
        .top-bar {
            background: #fff;
            border-bottom: 2px solid #ddd;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 8px 16px;
            flex-shrink: 0;
        }

        .top-bar .info { font-size: 13px; color: #c00; font-weight: 700; }
        .top-bar .q-number { font-size: 28px; font-weight: 900; color: #222; }

        .progress-box {
            background: #5aab3f;
            color: #fff;
            font-size: 14px;
            font-weight: 900;
            padding: 6px 16px;
            border-radius: 8px;
            min-width: 70px;
            text-align: center;
        }

        /* MIDDLE */
        .middle-section {
    display: flex;
    border-bottom: 2px solid #ddd;
    flex: 1;
    min-height: 250px;
    background: #fff;
}

        .img-side {
            flex: 0 0 50%;
            border-right: 2px solid #ddd;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding: 10px;
            background: #fafafa;
        }

        .img-side img {
            max-width: 100%;
            max-height: 240px;
            object-fit: contain;
        }

        .text-side {
    flex: 0 0 50%;
    padding: 16px 20px;
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
    background: #fff;
}

        .text-side-full {
    width: 100%;
    padding: 16px 20px;
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
    background: #fff;
}

        .text-side p, .text-side-full p {
            font-size: 15px;
            font-weight: 600;
            color: #222;
            line-height: 1.7;
        }

        /* OPTIONS */
        .options-section {
            background: #fff;
            border-top: 1px solid #111;
            border-bottom: 1px solid #111;
            flex-shrink: 0;
        }

        .option-row {
            display: flex;
            align-items: center;
            border-bottom: 1px solid #111;
            padding: 14px 16px;
            cursor: pointer;
            transition: background 0.15s;
            background: #e0e0e0;
        }

        .option-row:last-child { border-bottom: none; }
        .option-row:hover { background: #d0d0d0; }
        .option-row.selected { background: #9e9e9e; }

        .opt-letter { font-size: 20px; font-weight: 900; color: #111; width: 36px; flex-shrink: 0; }
        .opt-text { flex: 1; font-size: 14px; color: #333; }

        .opt-check {
            width: 22px; height: 22px;
            border: 2px solid #aaa;
            border-radius: 4px;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #fff;
            font-size: 14px;
            font-weight: 900;
            color: #fff;
        }

        .option-row.selected .opt-check { background: #616161; border-color: #616161; }

        /* BOTTOM STRIP */
        .bottom-strip {
            background: #d0d0d0;
            display: flex;
            align-items: center;
            padding: 6px 8px;
            gap: 4px;
            border-top: 2px solid #bbb;
            flex-shrink: 0;
            flex-wrap: nowrap;
            position: fixed;
            bottom: 50px; left: 0; right: 0;
            z-index: 100;
            height: 70px;
            overflow: hidden;
        }

        /* ARROW BUTTONS */
        .arrow-btn-wrap {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            width: 55px;
            height: 65px;
        }

        .arrow-btn {
            background: none;
            border: none;
            cursor: pointer;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 55px;
            height: 65px;
            overflow: visible;
        }

        .arrow-btn:hover { filter: brightness(1.15); }

        /* QUESTION NUMBER STRIP */
        .q-nums-wrap {
            display: flex;
            flex-wrap: nowrap;
            gap: 2px;
            align-items: center;
            flex: 1;
            min-width: 0;
            overflow-x: auto;
            overflow-y: visible;
            scrollbar-width: none;
            -ms-overflow-style: none;
            padding-bottom: 14px;
        }

        .q-nums-wrap::-webkit-scrollbar { display: none; }

        .q-dot {
            min-width: 0;
            flex: 1 1 0;
            height: 26px;
            border-radius: 3px;
            background: #ebebeb;
            border: 1px solid #bbb;
            color: #c00;
            font-size: 10px;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            flex-shrink: 0;
            transition: 0.15s;
            position: relative;
        }

        .q-dot:hover { background: #ebebeb; }

        .q-dot.active {
            background: #ebebeb;
            color: #c00;
            border-color: #bbb;
            position: relative;
        }

        .q-dot.active::after {
            content: '';
            position: absolute;
            bottom: -13px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 0;
            border-left: 10px solid transparent;
            border-right: 10px solid transparent;
            border-bottom: 13px solid #5aab3f;
        }

        .q-dot.answered {
            background: #ebebeb;
            color: #000;
            border-color: #bbb;
        }

        /* SUBMIT */
        .submit-btn {
            background: #5aab3f;
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 0 16px;
            height: 36px;
            font-size: 13px;
            font-weight: 700;
            cursor: pointer;
            flex-shrink: 0;
            display: none;
        }

        .submit-btn:hover { background: #4a9a32; }
        .submit-btn.visible { display: block; }

        /* POPUP */
        .popup-overlay {
            display: none;
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0,0,0,0.5);
            z-index: 999;
            align-items: center;
            justify-content: center;
        }

        .popup-overlay.show { display: flex; }

        .popup-box {
            background: #fff;
            border-radius: 14px;
            padding: 28px 32px;
            max-width: 380px;
            width: 90%;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }

        .popup-box h3 { font-size: 18px; font-weight: 800; color: #1a2236; margin-bottom: 10px; }
        .popup-box p { font-size: 14px; color: #64748b; margin-bottom: 22px; line-height: 1.6; }
        .popup-btns { display: flex; gap: 10px; justify-content: center; }

        .popup-btn-ok {
            background: #5aab3f; color: #fff; border: none;
            border-radius: 8px; padding: 10px 24px;
            font-size: 14px; font-weight: 700; cursor: pointer;
        }

        .popup-btn-cancel {
            background: #f1f5f9; color: #374151; border: none;
            border-radius: 8px; padding: 10px 24px;
            font-size: 14px; font-weight: 700; cursor: pointer;
        }

        .popup-btn-ok:hover { background: #4a9a32; }
        .popup-btn-cancel:hover { background: #e2e8f0; }

        @@media (max-width: 600px) {
            .middle-section { flex-direction: column; }
            .img-side { flex: none; width: 100%; border-right: none; border-bottom: 2px solid #ddd; max-height: 180px; overflow: hidden; }
            .text-side, .text-side-full { padding: 12px 14px; }
            .text-side p, .text-side-full p { font-size: 14px; }
            .q-dot { width: 24px; height: 24px; font-size: 10px; }
            .bottom-strip { padding: 6px 8px; gap: 4px; }
        }

        @@media (max-width: 380px) {
            .q-dot { width: 22px; height: 22px; font-size: 9px; }
        }
    </style>
</head>
<body>

<div class="popup-overlay" id="popupOverlay">
    <div class="popup-box">
        <h3>Submit Exam</h3>
        <p id="popupMsg">Are you sure you want to submit?</p>
        <div class="popup-btns">
            <button class="popup-btn-cancel" onclick="closePopup()">Continue Exam</button>
            <button class="popup-btn-ok" onclick="doSubmit()">Submit</button>
        </div>
    </div>
</div>

<form method="post" action="/Exam/Submit" id="examForm">
    @Html.AntiForgeryToken()
    <input type="hidden" name="moduleId" value="@moduleId" />
    <input type="hidden" name="sessionId" value="@sessionId" />

    <div class="page-wrapper">

        @for (int i = 0; i < Model.Count; i++)
        {
            var q = Model[i];
            <div id="qcard-@i" style="@(i == 0 ? "display:flex;" : "display:none;") flex-direction:column; width:100%;">

                <div class="top-bar">
                    <div class="info">Module @moduleId</div>
                    <div class="q-number">@(i + 1)</div>
                    <div class="progress-box" id="progressBox-@i">@(i+1) / @Model.Count</div>
                </div>

                <div class="middle-section">
                    @if (!string.IsNullOrEmpty(q.ImagePath))
                    {
                        <div class="img-side">
                            <img src="~/images/@q.ImagePath" />
                        </div>
                        <div class="text-side">
                            <p>@q.QuestionText</p>
                        </div>
                    }
                    else
                    {
                        <div class="text-side-full">
                            <p>@q.QuestionText</p>
                        </div>
                    }
                </div>

                <div class="options-section">
                    @if (!string.IsNullOrWhiteSpace(q.OptionA))
                    {
                        <div class="option-row" id="opt-@i-A" onclick="selectOption(@i, 'A', this)">
                            <input type="radio" name="answers[@q.Id]" value="A" style="display:none" />
                            <div class="opt-letter">A</div>
                            <div class="opt-text">@q.OptionA</div>
                            <div class="opt-check" id="chk-@i-A"></div>
                        </div>
                    }
                    @if (!string.IsNullOrWhiteSpace(q.OptionB))
                    {
                        <div class="option-row" id="opt-@i-B" onclick="selectOption(@i, 'B', this)">
                            <input type="radio" name="answers[@q.Id]" value="B" style="display:none" />
                            <div class="opt-letter">B</div>
                            <div class="opt-text">@q.OptionB</div>
                            <div class="opt-check" id="chk-@i-B"></div>
                        </div>
                    }
                    @if (!string.IsNullOrWhiteSpace(q.OptionC))
                    {
                        <div class="option-row" id="opt-@i-C" onclick="selectOption(@i, 'C', this)">
                            <input type="radio" name="answers[@q.Id]" value="C" style="display:none" />
                            <div class="opt-letter">C</div>
                            <div class="opt-text">@q.OptionC</div>
                            <div class="opt-check" id="chk-@i-C"></div>
                        </div>
                    }
                </div>

            </div>
        }

        <div class="bottom-strip">

           <div class="arrow-btn-wrap prev" onclick="goPrev()">
    <button type="button" class="arrow-btn">
        <svg width="55" height="60" viewBox="0 0 55 60" style="overflow:visible;display:block;">
            <path d="M48,8 C52,8 54,12 52,16 L32,50 C30,54 25,54 23,50 L3,16 C1,12 3,8 7,8 Z" fill="#5aab3f" transform="rotate(90,27.5,30)"/>
            <text x="27.5" y="30" text-anchor="middle" dominant-baseline="central" font-size="15" font-weight="900" fill="#fff" font-family="Arial" id="prevNum">-</text>
        </svg>
    </button>
</div>

            <div class="q-nums-wrap" id="qStrip">
                @for (int i = 0; i < Model.Count; i++)
                {
                    <div class="q-dot @(i == 0 ? "active" : "")" id="qnum-@i" onclick="goToQuestion(@i)">@(i + 1)</div>
                }
            </div>
<div class="arrow-btn-wrap next" onclick="goNext()">
    <button type="button" class="arrow-btn">
        <svg width="55" height="60" viewBox="0 0 55 60" style="overflow:visible;display:block;">
            <path d="M48,8 C52,8 54,12 52,16 L32,50 C30,54 25,54 23,50 L3,16 C1,12 3,8 7,8 Z" fill="#5aab3f" transform="rotate(-90,27.5,30)"/>
            <text x="27.5" y="30" text-anchor="middle" dominant-baseline="central" font-size="15" font-weight="900" fill="#fff" font-family="Arial" id="nextNum">-</text>
        </svg>
    </button>
</div>

            <button type="button" class="submit-btn" id="submitBtn" onclick="confirmSubmit()">Submit</button>
        </div>

    </div>
</form>

<script>
    let currentQ = 0;
    let totalQ = @Model.Count;
    let answered = new Array(totalQ).fill(false);
    let selectedOptions = new Array(totalQ).fill(null);

    function updateArrowNums() {
        document.getElementById('prevNum').textContent = currentQ > 0 ? currentQ : '-';
        document.getElementById('nextNum').textContent = currentQ < totalQ - 1 ? (currentQ + 2) : '-';
    }

    function checkAllAnswered() {
        let allDone = answered.every(a => a === true);
        let btn = document.getElementById('submitBtn');
        if (allDone) btn.classList.add('visible');
        else btn.classList.remove('visible');
    }

    function goToQuestion(index) {
        let prevCard = document.getElementById('qcard-' + currentQ);
        if (prevCard) prevCard.style.display = 'none';
        document.getElementById('qnum-' + currentQ).classList.remove('active');
        if (answered[currentQ]) document.getElementById('qnum-' + currentQ).classList.add('answered');

        currentQ = index;

        let nextCard = document.getElementById('qcard-' + currentQ);
        if (nextCard) nextCard.style.display = 'flex';

        let numEl = document.getElementById('qnum-' + currentQ);
        numEl.classList.remove('answered');
        numEl.classList.add('active');

        updateArrowNums();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    function goPrev() { if (currentQ > 0) goToQuestion(currentQ - 1); }
    function goNext() { if (currentQ < totalQ - 1) goToQuestion(currentQ + 1); }

    function selectOption(qIndex, option, rowEl) {
        if (selectedOptions[qIndex] === option) {
            rowEl.classList.remove('selected');
            rowEl.querySelector('input[type="radio"]').checked = false;
            rowEl.querySelector('.opt-check').innerText = '';
            selectedOptions[qIndex] = null;
            answered[qIndex] = false;
            let numEl = document.getElementById('qnum-' + qIndex);
            numEl.classList.remove('answered');
            if (qIndex === currentQ) numEl.classList.add('active');
            checkAllAnswered();
            return;
        }

        let card = document.getElementById('qcard-' + qIndex);
        card.querySelectorAll('.option-row').forEach(r => {
            r.classList.remove('selected');
            r.querySelector('.opt-check').innerText = '';
            r.querySelector('input[type="radio"]').checked = false;
        });

        rowEl.classList.add('selected');
        rowEl.querySelector('input[type="radio"]').checked = true;
        rowEl.querySelector('.opt-check').innerText = '✗';
        selectedOptions[qIndex] = option;
        answered[qIndex] = true;

        let numEl = document.getElementById('qnum-' + qIndex);
        numEl.classList.add('answered');
        if (qIndex === currentQ) numEl.classList.add('active');
        checkAllAnswered();
    }

    function confirmSubmit() {
        document.getElementById('popupMsg').innerText = 'All questions answered. Are you sure you want to submit?';
        document.getElementById('popupOverlay').classList.add('show');
    }

    function closePopup() { document.getElementById('popupOverlay').classList.remove('show'); }
    function doSubmit() { document.getElementById('examForm').submit(); }

    updateArrowNums();
</script>

</body>
</html>
