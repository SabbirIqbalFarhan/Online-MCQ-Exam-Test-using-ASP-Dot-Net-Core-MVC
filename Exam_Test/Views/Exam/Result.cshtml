@{
    ViewData["Title"] = "Exam Result";
    int moduleId = ViewBag.ModuleId ?? 0;
    int correct  = ViewBag.Correct  ?? 0;
    int wrong    = ViewBag.Wrong    ?? 0;
    int total    = correct + wrong;
    int pct      = total > 0 ? (correct * 100 / total) : 0;
    double dashOffset = 283 - (283.0 * pct / 100);
}

<style>
    body {
        margin: 0;
        font-family: "Segoe UI", Roboto, Arial, sans-serif;
        background: #f4f6ff;
        color: #111827;
    }

    .page {
        max-width: 700px;
        margin: auto;
        padding: 60px 20px;
        text-align: center;
    }

    .card {
        background: #fff;
        border: 1px solid #e5e7eb;
        border-radius: 18px;
        padding: 36px 30px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.05);
    }

    .title    { font-size: 22px; font-weight: 700; margin-bottom: 6px; }
    .subtitle { font-size: 13px; color: #6b7280; margin-bottom: 24px; }

    .score-ring-wrap { display: flex; justify-content: center; margin-bottom: 24px; }
    .score-ring { position: relative; width: 120px; height: 120px; }
    .score-ring svg { transform: rotate(-90deg); }
    .ring-bg   { fill: none; stroke: #e5e7eb; stroke-width: 10; }
    .ring-fill { fill: none; stroke: #10b981; stroke-width: 10; stroke-linecap: round; stroke-dasharray: 283; transition: stroke-dashoffset 1s ease; }
    .ring-text { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; }
    .ring-pct  { font-size: 22px; font-weight: 800; color: #111827; line-height: 1; }
    .ring-label{ font-size: 10px; color: #6b7280; font-weight: 600; margin-top: 2px; }

    .stats { display: flex; justify-content: center; gap: 16px; margin: 0 0 28px 0; flex-wrap: wrap; }
    .box { flex: 1; min-width: 130px; padding: 16px; border-radius: 14px; border: 1px solid #e5e7eb; background: #f9fafb; }
    .box h3 { margin: 0; font-size: 13px; color: #6b7280; font-weight: 500; }
    .box span { display: block; margin-top: 6px; font-size: 24px; font-weight: 800; }

    .correct-val { color: #16a34a; }
    .wrong-val   { color: #dc2626; }
    .total-val   { color: #4f46e5; }

    .divider { border: none; border-top: 1px solid #f3f4f6; margin: 0 0 20px 0; }

    .actions { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; }
    .btn {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 10px 18px;
        border-radius: 10px;
        font-size: 13px;
        font-weight: 600;
        text-decoration: none;
        transition: 0.2s;
    }
    .btn-primary { background: #4f46e5; color: #fff; }
    .btn-primary:hover { background: #3730a3; color: #fff; }
    .btn-outline { background: #fff; border: 1px solid #4f46e5; color: #4f46e5; }
    .btn-outline:hover { background: #eef2ff; }
    .btn-success { background: #10b981; color: #fff; }
    .btn-success:hover { background: #059669; color: #fff; }
</style>

<div class="page">
    <div class="card">

        <div class="title">🎯 Module @moduleId — Exam Result</div>
        <div class="subtitle">Here's how you performed on this module</div>

        <div class="score-ring-wrap">
            <div class="score-ring">
                <svg width="120" height="120" viewBox="0 0 100 100">
                    <circle class="ring-bg"   cx="50" cy="50" r="45" />
                    <circle class="ring-fill" cx="50" cy="50" r="45"
                            style="stroke-dashoffset:@dashOffset.ToString("F1")" />
                </svg>
                <div class="ring-text">
                    <span class="ring-pct">@pct%</span>
                    <span class="ring-label">Score</span>
                </div>
            </div>
        </div>

        <div class="stats">
            <div class="box"><h3>✅ Correct</h3><span class="correct-val">@correct</span></div>
            <div class="box"><h3>❌ Wrong</h3>  <span class="wrong-val">@wrong</span></div>
            <div class="box"><h3>📋 Total</h3>  <span class="total-val">@total</span></div>
        </div>

        <hr class="divider" />

        <div class="actions">
            <a class="btn btn-primary" href="/Exam/Review?moduleId=@moduleId">🔍 Review All</a>
            <a class="btn btn-outline" href="/Exam/Review?moduleId=@moduleId&onlyWrong=true">❌ Wrong Only</a>
            <a class="btn btn-success" href="/Exam/History">📊 History</a>
            <a class="btn btn-outline" href="/User/Dashboard">← Dashboard</a>
        </div>

    </div>
</div>