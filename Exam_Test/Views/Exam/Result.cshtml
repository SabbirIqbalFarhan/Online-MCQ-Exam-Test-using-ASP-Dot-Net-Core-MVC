@{
    ViewData["Title"] = "Exam Result";
    int moduleId = ViewBag.ModuleId ?? 0;
    int correct  = ViewBag.Correct  ?? 0;
    int wrong    = ViewBag.Wrong    ?? 0;
    int total    = correct + wrong;
    int pct      = total > 0 ? (correct * 100 / total) : 0;
    double dashOffset = 283 - (283.0 * pct / 100);
    var userAnswers = ViewBag.UserAnswers as List<Exam_Test.Models.UserAnswer> ?? new List<Exam_Test.Models.UserAnswer>();
}

<style>
    body { margin: 0; font-family: "Segoe UI", Roboto, Arial, sans-serif; background: #f4f6ff; color: #111827; }
    .page { max-width: 800px; margin: auto; padding: 40px 16px; }
    .card { background: #fff; border: 1px solid #e5e7eb; border-radius: 18px; padding: 32px 28px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); margin-bottom: 24px; text-align: center; }
    .title { font-size: 22px; font-weight: 700; margin-bottom: 6px; }
    .subtitle { font-size: 13px; color: #6b7280; margin-bottom: 24px; }
    .score-ring-wrap { display: flex; justify-content: center; margin-bottom: 24px; }
    .score-ring { position: relative; width: 120px; height: 120px; }
    .score-ring svg { transform: rotate(-90deg); }
    .ring-bg   { fill: none; stroke: #e5e7eb; stroke-width: 10; }
    .ring-fill { fill: none; stroke: #10b981; stroke-width: 10; stroke-linecap: round; stroke-dasharray: 283; }
    .ring-text { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; }
    .ring-pct  { font-size: 22px; font-weight: 800; color: #111827; line-height: 1; }
    .ring-label{ font-size: 10px; color: #6b7280; font-weight: 600; margin-top: 2px; }
    .stats { display: flex; justify-content: center; gap: 16px; margin: 0 0 28px 0; flex-wrap: wrap; }
    .box { flex: 1; min-width: 100px; padding: 16px; border-radius: 14px; border: 1px solid #e5e7eb; background: #f9fafb; }
    .box h3 { margin: 0; font-size: 12px; color: #6b7280; font-weight: 500; }
    .box span { display: block; margin-top: 6px; font-size: 24px; font-weight: 800; }
    .correct-val { color: #16a34a; }
    .wrong-val   { color: #dc2626; }
    .total-val   { color: #4f46e5; }
    .divider { border: none; border-top: 1px solid #f3f4f6; margin: 0 0 20px 0; }
    .actions { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; }
    .btn { display: inline-flex; align-items: center; gap: 6px; padding: 10px 18px; border-radius: 10px; font-size: 13px; font-weight: 600; text-decoration: none; transition: 0.2s; }
    .btn-outline { background: #fff; border: 1px solid #e5e7eb; color: #374151; }
    .btn-outline:hover { background: #f3f4f6; }
    .section-title { font-size: 16px; font-weight: 700; color: #1a2236; margin-bottom: 16px; }
    .q-row { background: #fff; border: 1px solid #e5e7eb; border-radius: 14px; padding: 20px 22px; margin-bottom: 14px; }
    .q-row.correct-row { border-left: 4px solid #10b981; }
    .q-row.wrong-row   { border-left: 4px solid #ef4444; }
    .q-status { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.7px; margin-bottom: 10px; }
    .q-status.c { color: #10b981; }
    .q-status.w { color: #ef4444; }
    .q-image { max-width: 100%; max-height: 200px; border-radius: 8px; border: 1px solid #e5e7eb; margin-bottom: 12px; display: block; }
    .q-text { font-size: 14px; font-weight: 600; color: #1a2236; margin-bottom: 14px; line-height: 1.6; }
    .option-row { display: flex; align-items: center; gap: 10px; padding: 10px 14px; border-radius: 10px; border: 1.5px solid #e5e7eb; margin-bottom: 8px; font-size: 13px; color: #374151; }
    .option-row.opt-correct { background: #dcfce7; border-color: #86efac; color: #166534; font-weight: 700; }
    .option-row.opt-wrong   { background: #fee2e2; border-color: #fca5a5; color: #991b1b; font-weight: 700; }
    .opt-letter { width: 28px; height: 28px; border-radius: 50%; background: #f3f4f6; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 12px; flex-shrink: 0; }
    .opt-letter.correct-letter { background: #10b981; color: #fff; }
    .opt-letter.wrong-letter   { background: #ef4444; color: #fff; }
    .opt-tag { margin-left: auto; font-size: 11px; font-weight: 700; padding: 2px 8px; border-radius: 999px; }
    .tag-your-correct { background: #dcfce7; color: #166534; }
    .tag-your-wrong   { background: #fee2e2; color: #991b1b; }
    .tag-correct-ans  { background: #dbeafe; color: #1d4ed8; }
</style>

<div class="page">
    <div class="card">
        <div class="title">🎯 Module @moduleId — Exam Result</div>
        <div class="subtitle">Here's how you performed</div>

        <div class="score-ring-wrap">
            <div class="score-ring">
                <svg width="120" height="120" viewBox="0 0 100 100">
                    <circle class="ring-bg" cx="50" cy="50" r="45" />
                    <circle class="ring-fill" cx="50" cy="50" r="45" style="stroke-dashoffset:@dashOffset.ToString("F1")" />
                </svg>
                <div class="ring-text">
                    <span class="ring-pct">@pct%</span>
                    <span class="ring-label">Score</span>
                </div>
            </div>
        </div>

        <div class="stats">
            <div class="box"><h3>✅ Correct</h3><span class="correct-val">@correct</span></div>
            <div class="box"><h3>❌ Wrong</h3><span class="wrong-val">@wrong</span></div>
            <div class="box"><h3>📋 Total</h3><span class="total-val">@total</span></div>
        </div>

        <hr class="divider" />

        <div class="actions">
            <a class="btn btn-outline" href="/User/Dashboard">← Back to Dashboard</a>
        </div>
    </div>

    <div class="section-title">📋 Question Breakdown</div>

    @foreach (var ans in userAnswers)
    {
        var q = ans.Question;
        if (q == null) continue;

        var options = new[]
        {
            new { Letter = "A", Text = q.OptionA },
            new { Letter = "B", Text = q.OptionB },
            new { Letter = "C", Text = q.OptionC }
        };

        <div class="q-row @(ans.IsCorrect ? "correct-row" : "wrong-row")">
            <div class="q-status @(ans.IsCorrect ? "c" : "w")">
                @(ans.IsCorrect ? "✅ Correct" : "❌ Wrong")
            </div>

            @if (!string.IsNullOrEmpty(q.ImagePath))
            {
                <img class="q-image" src="~/images/@q.ImagePath" />
            }

            @if (!string.IsNullOrEmpty(q.QuestionText))
            {
                <div class="q-text">@q.QuestionText</div>
            }

            @foreach (var opt in options)
            {
                bool isCorrectOpt = q.CorrectAnswer == opt.Letter;
                bool isSelectedOpt = ans.SelectedAnswer == opt.Letter;

                string rowClass = "";
                string letterClass = "";
                string tag = "";

                if (isSelectedOpt && ans.IsCorrect)
                {
                    rowClass = "opt-correct";
                    letterClass = "correct-letter";
                    tag = "<span class='opt-tag tag-your-correct'>✓ Your Answer</span>";
                }
                else if (isSelectedOpt && !ans.IsCorrect)
                {
                    rowClass = "opt-wrong";
                    letterClass = "wrong-letter";
                    tag = "<span class='opt-tag tag-your-wrong'>✗ Your Answer</span>";
                }
                else if (isCorrectOpt && !ans.IsCorrect)
                {
                    rowClass = "opt-correct";
                    letterClass = "correct-letter";
                    tag = "<span class='opt-tag tag-correct-ans'>✓ Correct Answer</span>";
                }

                <div class="option-row @rowClass">
                    <div class="opt-letter @letterClass">@opt.Letter</div>
                    <span>@opt.Text</span>
                    @Html.Raw(tag)
                </div>
            }
        </div>
    }
</div>